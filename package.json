{
  "name": "merge-funding",
  "version": "1.0.0",
  "description": "Funding Pips Application",
  "scripts": {
    "install:all": "npm install && cd backendFundingPips && npm install && cd ../FundingPipscopy && npm install",
    "build:frontend": "cd FundingPipscopy && npm run build",
    "build:backend": "cd backendFundingPips && nest build",
    "build": "npm run build:frontend && npm run build:backend",
    "start": "cd backendFundingPips && npm run start:prod"
  }
} 